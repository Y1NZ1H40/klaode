# texts/

把你想展示的 `.txt` 文件放在这个目录下，运行 `klaode 文件名.txt` 即可打开。

如果目录下只有一个 `.txt` 文件，可以直接运行 `klaode` 而不用带文件名。

## 插入代码片段

在文本中使用 `{{code:文件名}}` 占位符，klaode 会读取 `snippets/` 目录下对应的
代码文件并替换成带语法高亮的代码块，从而模拟"AI 正在写代码"的对话效果。

例如：

```
这是关于这段代码的说明：

{{code:example.py}}

以上就是修复后的实现。
```

参考 `example.txt` 的写法。
